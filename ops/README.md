# ops

Локальный стенд: `compose.yaml` с профилями `infra` | `edge` | `full`, конфиг Centrifugo, Caddyfile,
инициализация баз и генератор ключей.

Документация — [`docs/operations/local-stack.md`](../docs/operations/local-stack.md).

```bash
docker compose --profile infra up -d
```
