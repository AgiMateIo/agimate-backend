package ru.agimate.controlapi.controller.manage.dto.agentrequest;

/** The two sides of a thread: what the asker handed over, and what the callee answered. */
public enum AgentRequestExchangeKind {
    REQUEST,
    REPORT
}
