<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="create-skills-table" author="agimate">
        <createTable tableName="skills">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="TEXT"/>
            <!-- Тело SKILL.md без frontmatter (заголовков). Источник истины контента скилла. -->
            <column name="md_content" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <!-- Коннекторы, которые требует скилл (заменяет таблицу skill_connectors). -->
            <column name="connector_codes" type="text[]" defaultValueComputed="'{}'::text[]">
                <constraints nullable="false"/>
            </column>
            <column name="version" type="INTEGER" defaultValueNumeric="1">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="is_public" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="deleted_at" type="TIMESTAMP"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Unique name per user among non-deleted skills (soft-delete aware). -->
        <sql>CREATE UNIQUE INDEX uq_skills_user_name_active ON skills (user_id, name) WHERE deleted_at IS NULL</sql>
        <!-- Фильтр списков по коннектору (connector_codes @>/array_position). -->
        <sql>CREATE INDEX idx_skills_connector_codes ON skills USING GIN (connector_codes)</sql>
    </changeSet>

</databaseChangeLog>
