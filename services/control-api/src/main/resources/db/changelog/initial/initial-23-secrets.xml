<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!--
        Общий стор обратимых секретов (envelope-шифрование). Per-row DEK шифрует encrypted_data
        (AES-256-GCM, IV = iv); DEK шифруется KEK (один источник) с AAD = entity + owner_id и лежит
        в encrypted_dek. owner_id в таблице НЕ хранится — это привязка, передаётся вызывающим.
    -->
    <changeSet id="create-secrets-table" author="agimate">
        <createTable tableName="secrets">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="entity" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="encrypted_dek" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="iv" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="encrypted_data" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="last_used_at" type="TIMESTAMP"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>
