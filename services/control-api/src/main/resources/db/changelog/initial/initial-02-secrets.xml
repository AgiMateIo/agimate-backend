<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!-- Envelope-encrypted secret store. Per-row DEK encrypts encrypted_data; the DEK itself is
         KEK-encrypted with AAD = entity + owner_id. owner_id is NOT stored — the caller supplies it. -->
    <changeSet id="create-secrets-table" author="agimate">
        <createTable tableName="secrets">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="entity" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="encrypted_dek" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="iv" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="encrypted_data" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="last_used_at" type="TIMESTAMP"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>
