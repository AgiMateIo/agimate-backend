<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!--
      Канонический full-fidelity журнал ходов рана: по одной строке на AgentChatMessage воркера
      (assistant/tool), без капов — в отличие от капнутой канальной проекции channel_session_messages.
      Пишется для всех ранов, включая direct (session_id NULL). Идемпотентность — (run_id, turn_index):
      SaveTurn идёт обычным (не durable) вызовом, реплей/ретрай повторяет ту же пару без дубля.
      session_id денормализован без логики непрерывности — AgentSession отложен.
      finish_reason/model/call_id заводятся nullable здесь; протяжка (1b) без миграции.
    -->
    <changeSet id="create-agent-run-turns-table" author="agimate">
        <createTable tableName="agent_run_turns">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="run_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="session_id" type="UUID"/>
            <column name="agent_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="turn_index" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="role" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="text" type="TEXT"/>
            <column name="thinking" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="tool_calls" type="JSONB"/>
            <column name="tool_results" type="JSONB"/>
            <column name="finish_reason" type="TEXT"/>
            <column name="model" type="TEXT"/>
            <column name="call_id" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE agent_run_turns
                ADD CONSTRAINT chk_agent_run_turns_role
                    CHECK (role IN ('SYSTEM', 'USER', 'ASSISTANT', 'TOOL'));
        </sql>

        <addForeignKeyConstraint baseTableName="agent_run_turns"
                                 baseColumnNames="run_id"
                                 constraintName="fk_agent_run_turns_run"
                                 referencedTableName="agent_runs"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>

        <addForeignKeyConstraint baseTableName="agent_run_turns"
                                 baseColumnNames="agent_id"
                                 constraintName="fk_agent_run_turns_agent"
                                 referencedTableName="agents"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>

        <addUniqueConstraint tableName="agent_run_turns"
                             columnNames="run_id, turn_index"
                             constraintName="uq_agent_run_turns_run_turn"/>
    </changeSet>

</databaseChangeLog>
