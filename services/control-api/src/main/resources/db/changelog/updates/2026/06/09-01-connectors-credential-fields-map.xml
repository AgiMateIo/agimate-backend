<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!--
        credential_fields сменил формат: JSON-массив кодов полей → JSON-объект «код → название».
        Старые значения-массивы больше не десериализуются в Map<String,String> — зачищаем,
        ConnectorBootstrap перезапишет актуальными значениями на ближайшем старте.
    -->
    <changeSet id="connectors-credential-fields-to-map" author="agimate">
        <sql>
            UPDATE connectors
            SET credential_fields = NULL
            WHERE credential_fields IS NOT NULL
              AND jsonb_typeof(credential_fields) = 'array';
        </sql>
    </changeSet>

</databaseChangeLog>
