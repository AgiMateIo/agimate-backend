<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Хвост 10-01: CHECK на kind из initial-07 знал только REQUEST/RESPONSE —
         расширяем на v2-виды (SaveMessage). -->
    <changeSet id="2026-07-10-02-message-kind-check" author="agimate">
        <sql>
            ALTER TABLE channel_session_messages
                DROP CONSTRAINT chk_channel_session_messages_kind;
            ALTER TABLE channel_session_messages
                ADD CONSTRAINT chk_channel_session_messages_kind
                    CHECK (kind IN ('REQUEST', 'RESPONSE', 'INBOUND', 'PROGRESS', 'ANSWER', 'ERROR'));
        </sql>
    </changeSet>

</databaseChangeLog>
