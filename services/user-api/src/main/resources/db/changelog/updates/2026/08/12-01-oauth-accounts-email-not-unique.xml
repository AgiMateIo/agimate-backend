<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="oauth-accounts-email-not-unique" author="Eugene Smith">
        <comment>
            One person signing in through a second provider with the same mailbox gets a second row
            here, and the unique email blocked exactly that. Identity in this table is
            (oauth_provider, provider_user_id) — it stays unique; one account per address is still
            enforced by users.email.
        </comment>
        <dropUniqueConstraint tableName="user_oauth_accounts"
                              constraintName="user_oauth_accounts_email_key"/>
    </changeSet>

</databaseChangeLog>
