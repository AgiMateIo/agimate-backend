<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!-- Identity here is (oauth_provider, provider_user_id), and email is deliberately not unique:
         one person signing in through a second provider with the same mailbox gets a second row.
         One account per address is still enforced by users.email. -->
    <changeSet id="create-user-oauth-accounts-table" author="Eugene Smith">
        <createTable tableName="user_oauth_accounts">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="oauth_provider" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="provider_user_id" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="email" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="first_name" type="TEXT"/>
            <column name="last_name" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="user_oauth_accounts"
                baseColumnNames="user_id"
                constraintName="fk_user_oauth_accounts_user"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <createIndex tableName="user_oauth_accounts" indexName="idx_user_oauth_accounts_user_id">
            <column name="user_id"/>
        </createIndex>

        <addUniqueConstraint tableName="user_oauth_accounts"
                             columnNames="oauth_provider, provider_user_id"
                             constraintName="uq_user_oauth_accounts_oauth_provider_provider_user_id"/>
    </changeSet>

</databaseChangeLog>
