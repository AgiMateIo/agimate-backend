<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!-- referred_by is a snapshot of the moment the account was created and never changes afterwards
         — an existing user following somebody's link must not be re-attributed, and that rule lives
         in the code, not in this DDL. referral_code is handed to every user whether or not they ever
         invite anybody, which is what makes NOT NULL possible. -->
    <changeSet id="create-users-table" author="Eugene Smith">
        <createTable tableName="users">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="referred_by" type="UUID"/>
            <column name="email" type="TEXT">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_users_email"/>
            </column>
            <column name="first_name" type="TEXT"/>
            <column name="last_name" type="TEXT"/>
            <column name="display_name" type="TEXT"/>
            <column name="role" type="TEXT" defaultValue="GUEST">
                <constraints nullable="false"/>
            </column>
            <column name="referral_code" type="TEXT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint tableName="users" columnNames="referral_code"
                             constraintName="uq_users_referral_code"/>

        <addForeignKeyConstraint baseTableName="users" baseColumnNames="referred_by"
                                 referencedTableName="users" referencedColumnNames="id"
                                 constraintName="fk_users_referred_by"/>

        <createIndex tableName="users" indexName="idx_users_referred_by">
            <column name="referred_by"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
